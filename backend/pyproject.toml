[project]
name = "agent-backend"
version = "0.7.1"
requires-python = ">=3.12"
dependencies = [
    # Два движка — два SDK. В каждом лежит бинарник своего CLI, поэтому образ
    # везёт обе подписки сразу: переключение движка не требует пересборки.
    "claude-agent-sdk>=0.2.131",
    "openai-codex>=0.144.4",
    "fastapi>=0.115",
    "uvicorn[standard]>=0.34",
    "websockets>=14.0",
    "httpx>=0.27",
    "python-multipart>=0.0.9",
    "croniter>=6.2.2",
    "mcp>=1.9",
    # Живая синхронизация задач и вики. Раньше приезжал транзитом через mcp —
    # зависимость прямая, пусть так и будет записана.
    "sse-starlette>=2.1",
    "pypdf>=6.15.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
